import test from "node:test"; import assert from "node:assert/strict"; import { resourceState, recommend, authorizeTool } from "../packages/workbench/src/policy.mjs";
test("unverified and unavailable resources never appear available",()=>{assert.equal(resourceState({online:true,authorized:true,quota:true}),"PENDING");assert.equal(resourceState({online:true,authorized:true,quota:true,lastTested:"now"}),"AVAILABLE");assert.equal(resourceState({online:false}),"OFFLINE");assert.equal(resourceState({online:true,authorized:true,quota:false}),"QUOTA_EXCEEDED");});
test("tenant isolation and free-first recommendation",()=>{const rows=[{id:"paid",tenant_ids:["a"],tools:["tests"],state:"AVAILABLE",cost_tier:"paid",health_score:1},{id:"free",tenant_ids:["a"],tools:["tests"],state:"AVAILABLE",cost_tier:"free",health_score:2},{id:"other",tenant_ids:["b"],tools:["tests"],state:"AVAILABLE",cost_tier:"free",health_score:9}];assert.deepEqual(recommend(rows,{tenantId:"a",requiredTools:["tests"]}).map(x=>x.id),["free"]);});
test("tool policy denies unlisted operations",()=>{assert.equal(authorizeTool("tests",["tests","evidence"]),true);assert.equal(authorizeTool("deploy",["tests","evidence"]),false);});
